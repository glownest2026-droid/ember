import { Fredoka } from 'next/font/google';

/** Playful childlike voice for discover “why it matters” quotes. */
export const discoverPlayful = Fredoka({
  subsets: ['latin'],
  weight: ['400', '500', '600'],
  variable: '--font-discover-playful',
  display: 'swap',
});
